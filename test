local handle = io.popen("hostname")
local hostname = handle:read("*a")
handle:close()

hostname = string.gsub(hostname, "\n", "")

if hostname == "LAPTOP-2QC5QJ83" then
    showMessage("檢測到用戶,你是咖波:)")
else
    showMessage("你不是咖波:(")
end
